
- In the `get_commits` function, add handling for the case where the branch passed in is the same as the current branch.
- In the `create_pull_request` function, add a check to ensure that the `commits` list is not empty.

